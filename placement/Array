array   -we can store similer type of data in array.
		-data store in contigiuous memory.(in sequence but in range of datatype)
		-we can access data array using index.
		 
		 syntax:- int arr[];    (decleration)
				   
				  int arr[3];	(initialisation)
				  
				    a[0]=5;
				    a[1]=6;
				    a[2]=8;
					or
					int arr[3]={5,6,8}; 
					
		
				  